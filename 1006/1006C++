#include <iomanip>
#include <iostream>
 
int main() {
 
    double a, b, c;
    
    std::cin >> a >> b >> c;
    
    double media = ((a * 2) + (b * 3) + (c * 5)) /10;
    
    std::cout << std::fixed << std::setprecision(1);
    std::cout << "MEDIA = " << media << std::endl;
 
    return 0;
}