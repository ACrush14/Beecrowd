#include <stdio.h>
#include <iostream>
#include <iomanip>

 
int main() {
 
    int tempo, vel;
    double res;
    
    std::cin >> tempo >> vel;
    
    res = (tempo * vel) / 12.0;
    
    std::cout << std::fixed << std::setprecision(3) << res << std::endl;
    
 
    return 0;
}