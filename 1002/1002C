#include <stdio.h>
 
int main() {
    double raio;
    
    scanf("%lf", &raio);
    
    const double PI = 3.14159;
    
    double area = PI * raio * raio;
    
    printf("A=%.4f\n", area);

    return 0;
}