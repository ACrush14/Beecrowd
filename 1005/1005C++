#include <stdio.h>
#include <iomanip>
#include <iostream>
 
int main() {
 
    double a, b;
    
    std::cin >> a >> b;
    
    double media = ((a * 3.5) + (b * 7.5)) / 11;
    
    std::cout << std::fixed << std::setprecision(5) << "MEDIA = " << media << std::endl;

    return 0;
}