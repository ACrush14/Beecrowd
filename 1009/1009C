#include <stdio.h>
 
int main() {
 
    char nome[50];
    double sal, vendas, bonus, total;
    
    scanf("%s", nome);
    
    scanf("%lf %lf", &sal, &vendas);
    
    bonus = vendas * 0.15;
    
    total = sal + bonus;
    
    
    printf("TOTAL = R$ %.2f\n", total);
 
    return 0;
}