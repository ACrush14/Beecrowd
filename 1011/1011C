#include <stdio.h>
#include <math.h>
 
int main() {
 
    double pi = 3.14159;
    double volume;
    int r;
    scanf("%d", &r);
    
    volume = ((4.0/3.0) * pi * pow(r, 3));
    
    printf("VOLUME = %.3f\n", volume);
 
    return 0;
}